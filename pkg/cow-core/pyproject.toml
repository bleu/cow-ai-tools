[tool.poetry]
name = "cow-core"
version = "0.1.0"
description = "Shared core (logger) for CoW AI tools"
authors = ["José Ribeiro <me@joseribeiro.dev>"]

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
