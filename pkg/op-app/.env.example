# Copy to .env and set your keys: cp .env.example .env
# Used by python-dotenv when running the API (op_app/api.py, cow_brains, op_brains).

# CoW project (PROJECT=cow): required for Gemini chat + embeddings
GOOGLE_API_KEY=your_google_api_key_here

# Optional: project and paths (defaults work when running from pkg/op-app)
# PROJECT=cow
# OP_CHAT_BASE_PATH=../../data
