# CoW Protocol project – chat + embeddings use Gemini only (no OpenAI).
# Copy: cp .envrc.example.cow .envrc  then set GOOGLE_API_KEY.

export PROJECT=cow
# Or legacy: export USE_COW=true

export GOOGLE_API_KEY=your_google_api_key_here
export CHAT_MODEL=gemini-2.0-flash
# Embeddings default to gemini-embedding-001 (same API key).
# Rebuild FAISS after changing embedding model: PROJECT=cow poetry run python -m cow_brains.build_faiss

export OP_CHAT_BASE_PATH=../../data
# Optional overrides:
# export COW_DOCS_PATH=../../data/cow-docs/cow_docs.txt
# export COW_SWAP_DOCS_PATH=../../data/cow-docs/cow_swap_docs.txt
# export COW_SDK_DOCS_PATH=../../data/cow-docs/cow_sdk_docs.txt
# export COW_FAISS_PATH=../../data/cow-docs/faiss
# export COW_OPENAPI_PATH=../../data/cow-docs/openapi.yml
